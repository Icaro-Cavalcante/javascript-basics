// For loop - Executa um código um número limitado de vezes

for (let i = 0; i < 20 + 1; i+=1) {
    if (i % 2 == 0) {
        console.log(`${i} é par.`)
    }
    else {
        console.log(`${i} é ímpar.`)
    }
}